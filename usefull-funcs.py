round() #rounds to a whole number x.5 rounds to the nearest even number
abs() #returns the absolute(positive) value of a number
pow() #returns the value of x to the power of y
max() #returns the largest of the given numbers
bin() #returns the binary version of a number
print("text", "ali", end="\n", sep="     ") #end is what is printed at the end of the line, sep is what is printed between items
enumerate(name_of_list) #returns 2 number, index and the itemiself
cat = ['fat', 'gray', 'loud']
for index, item in enumerate(cat):
    print(index, item)